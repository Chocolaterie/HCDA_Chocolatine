<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    tools:context=".demonav.DemoNavTwoActivity">

    <TextView
        android:text="Je pense que je vais aller aux urgences"
        android:padding="10dp"
        android:textSize="40sp"
        android:textAlignment="center"
        android:layout_width="match_parent"
        android:layout_height="wrap_content" />

    <!-- Bouton pour relancer une nouvelle Activité DemoNavOne -->
    <Button
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Ouvrir One Activity"
        android:onClick="onClickOpenOneActivity"
        />

    <!-- Bouton  fermer toute les activités de la pile jusqu'a la première -->
    <Button
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Tout fermer sauf la première pile"
        android:onClick="onClickCloseAllToRoot"
        />

</LinearLayout>